services:
  - type: web
    name: ffmpeg-server
    env: python
    buildCommand: "apt-get install -y ffmpeg fonts-noto-cjk && pip install -r requirements.txt"
    startCommand: "gunicorn main:app --bind 0.0.0.0:8080 --timeout 300"
    plan: free
